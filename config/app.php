<?php
/**
 * Application paths.
 *
 * Leave base_path empty to auto-detect /portfolio/ style installs from the URL.
 * Set explicitly (example: '/portfolio') if your host reports odd SCRIPT_NAME values.
 */
declare(strict_types=1);

return [
    'base_path' => '',
];
